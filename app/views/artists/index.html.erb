
<% @artists.each do |artist| %>
  <h2> <%= artist.name %> </h2>
  <h4><%= pluralize(artist.song_count, 'song') %>:</h4>
  <ul>
    <% artist.songs.each do |song| %>
      <li><%= link_to "#{song.title}", song_path(song) %></li>
    <% end %>
  </ul>
<% end %>
